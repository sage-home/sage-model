/* 
 * Legacy implementation now forwards to new abstraction layer.
 * This file provides backward compatibility through macros defined in core_mymalloc.h.
 * All legacy functions are now implemented as macros that forward to the new sage_* functions.
 * This file is kept for potential future use and to maintain the build system structure.
 */

/* No actual function implementations needed - everything is handled by macros in the header */
